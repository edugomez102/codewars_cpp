#pragma once

#include <vector>

/**
 * @brief clean an array of numbers 
 *
 * @param input input vector of numbers
 * @param times the number of times that a number can be repeated
 */
std::vector<int> cleanRepeated(const std::vector<int>& input, int times);

