#pragma once

#include <vector>

/**
 * @brief true if walk is possible
 *
 * @param  directions array, must be s, n, e, w
 * @return true if possible
 */
bool isValidWalk(const std::vector<char>& directions);
